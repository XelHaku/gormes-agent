---
title: "Phase 5 — The Final Purge"
weight: 60
---

# Phase 5 — The Final Purge (100% Go)

**Status:** 🔨 in progress

**Deliverable:** Python tool scripts ported to Go or WASM. Python disappears entirely from the runtime path.

Phase 5 is when Python disappears entirely from the runtime path. Each sub-phase is a separable spec.

## Phase 5 Sub-phase Outline

| Subphase | Status | Deliverable |
|---|---|---|
| 5.A — Tool Surface Port | ✅ complete | `internal/tools` now ships the Go-native registry row (`ToolEntry`), toolset-scoped descriptor filtering, env/check-fn availability gates, and default runtime wiring for the built-in + delegation toolsets. |
| 5.B — Sandboxing Backends | ✅ complete | `internal/tools/environments/{daytona,docker,modal,singularity}` now cover the tracked 5.B backend seams with tested resource normalization, snapshot restore fallback, exec translation, lifecycle cleanup, Docker hardening/persistence mounts, and Singularity scratch/overlay management. SSH and shared file-sync remain future follow-on ports outside the current checklist. |
| 5.C — Browser Automation | ✅ complete | `internal/tools/browser_{chromedp,rod}.go` now cover the tracked Go-native `browser_navigate` scope with shared `driver=chromedp|rod` selection, local Chrome/Chromium launch, remote `BROWSER_CDP_URL` attach support, and deterministic metadata/HTML capture. Cloud-provider/browserbase/camofox parity remains future follow-on scope outside the current checklist. |
| 5.D — Vision + Image Generation | ✅ complete (tracked scope) | `internal/hermes` now translates canonical text+image message parts across the default HTTP client plus OpenRouter, Codex Responses, Anthropic, Gemini/Google Code Assist, and Bedrock request builders. Dedicated `tools/vision_tools.py` + `tools/image_generation_tool.py` ports remain future follow-on scope outside this checklist item. |
| 5.E — TTS / Voice / Transcription | ✅ complete (tracked scope) | `internal/gateway/voice_mode.go` now ports the shared `/voice` control plane with persisted per-chat `off/voice_only/all` state at `${XDG_DATA_HOME}/gormes/gateway_voice_mode.json`; STT/TTS/transcription engines remain explicit follow-on sidecar scope |
| 5.F — Skills System (Remaining) | 🔨 in progress | `internal/skills` now ships the typed skill-registry layer (`SkillMeta`, `SkillBundle`, `SkillReadinessStatus`, `HubLockFile`), deterministic filesystem/static source discovery, the canonical bundled/official/Claude Marketplace/ClawHub/GitHub/Hermes Index/LobeHub/skills.sh registry catalog, and the first local hub/install runtime. Skill auto-discovery is now live too: the runtime discovers the active store plus configured external skill trees with local-name precedence, writes `${XDG_DATA_HOME}/gormes/.skills_prompt_snapshot.json` metadata for the selected prompt block, and `cmd/gormes/{main,gateway,telegram}.go` plus the BOOT hook now wire live kernels through the shared skills runtime and usage logger. Remaining work is security/audit/update flows and broader category backfill. **Note:** Core learning loop (skill extraction, improvement) is Phase 2.G (P0) |
| 5.F.1 — Skills Hub Integration | ✅ complete | `cmd/gormes/skills.go` now consumes the shipped registry catalog format through `gormes skills sync`, `gormes skills list [--source=hub]`, and `gormes skills install <ref>`, using the local `.hub` cache + lock file under `${XDG_DATA_HOME}/gormes/skills/` |
| 5.F.2 — Skill Auto-Discovery | ✅ complete | `internal/skills` now auto-discovers the active store plus configured external skill directories, preserves local precedence over external duplicates by skill name, persists `${XDG_DATA_HOME}/gormes/.skills_prompt_snapshot.json` for the selected prompt block metadata, and `cmd/gormes/{main,gateway,telegram}.go` plus `internal/gateway/boot_hook.go` now wire the live kernels through the shared runtime |
| 5.G — MCP Integration | ✅ complete (tracked scope) | `internal/mcp/{client,oauth}.go` now ship the Go-native stdio MCP client core plus a generic OAuth 2.1 PKCE browser-callback flow with persisted per-server token/client state under `${XDG_DATA_HOME}/gormes/mcp-tokens/` and refresh-backed bearer reuse. HTTP transport, dynamic toolset injection, server mode, and sampling remain future follow-on scope outside the tracked checklist. |
| 5.H — ACP Integration | ✅ complete (tracked scope) | `internal/acp/{server,kernel_session}.go` now ship the stdio ACP baseline (`initialize`, `session/new`, `session/prompt`, `session/cancel`, `session/close`), `cmd/gormes/acp.go` exposes `gormes acp`, and `acp_registry/{agent.json,icon.svg}` advertise the editor manifest. Session/load, richer content blocks, and ACP client fs/terminal callback bridges remain follow-on scope. |
| 5.I — Plugins Architecture | ✅ complete | `internal/plugins/manifest.go` still owns the typed `plugin.yaml` contract, and `internal/plugins/runtime.go` now completes the runtime catalog: bundled/user/project manifest discovery, env-readiness reporting, general-plugin enable/disable state, memory/context provider selection, and the `gormes plugins list` operator surface backed by `${XDG_DATA_HOME}/gormes/plugins`. Concrete memory/context plugin ports remain tracked as their own follow-on rows under 5.I. |
| 5.J — Approval / Security Guards | ✅ complete | `internal/tools/approval.go` now performs fail-closed dangerous-command scans on command-like tool payloads, and both `internal/tools/executor.go` and `internal/kernel/toolexec.go` block execution before `Tool.Execute`. Interactive terminal approval UX remains future follow-on work once the broader Phase 5.K terminal/code-exec surface lands. |
| 5.K — Code Execution | ✅ complete (tracked scope) | `internal/tools/code_execution.go` now ports the tracked one-shot code-exec seam: Go-native `execute_code`, `ProcessRegistry` lifecycle state, scrubbed `strict`/`project` workspaces, and default registry wiring in `cmd/gormes/registry.go`. Interactive debug sessions and recursive in-script tool RPC remain follow-on scope outside this checklist. |
| 5.L — File Ops + Patches | ✅ complete (tracked scope) | `internal/tools/checkpoints.go` now ports the atomic shadow-repo checkpoint core with per-turn dedup, list/diff/restore flows, and pre-rollback snapshots under `${XDG_DATA_HOME}/gormes/checkpoints`; broader file read/write/patch tool ports remain future follow-on scope outside the tracked checklist. |
| 5.M — Mixture of Agents | ✅ complete (tracked scope) | `internal/tools/mixture_of_agents.go` now ports the tracked `mixture_of_agents` seam with concurrent OpenRouter reference-model fanout, deterministic aggregation prompt construction, structured partial-failure reporting, and default-registry wiring in `cmd/gormes/registry.go`; retry/backoff tuning and deeper multi-layer MoA variants remain follow-on scope. |
| 5.N — Misc Operator Tools | 🔨 in progress | `internal/tools/clarify.go` now ports the Go-native clarify tool core with request/choice validation, 120s timeout, structured replies, and dormant default-registry wiring in `cmd/gormes/registry.go`; `internal/tools/debug_helpers.go` now ports the shared redacting `DebugSession` helper with opt-in env gating plus explicit-save persistence under `${XDG_DATA_HOME}/gormes/logs`; `internal/tools/todo.go` now ports the per-session Todo tool core with a `TodoStore` (replace vs merge-by-id writes, duplicate-id collapsing, defensive-copy reads, status normalization, and `FormatForInjection` filtering for post-compression replay) plus a dormant default-registry entry in `cmd/gormes/registry.go` gated on an injected per-session store; `session_search`, `send_message`, `cronjob`, and `interrupt` remain follow-on scope. |
| 5.O — Hermes CLI Parity | 🔨 in progress | `internal/cli/hermes_tree.go` now ports the 49-file `hermes_cli/` tree as a typed parity manifest (`HermesCLITree`/`HermesCLIFile`/`HermesCLIPortStatus`) backed by `testdata/hermes_cli_tree.json`. Every upstream `hermes_cli/*.py` (49 files) is enumerated with a `ported`/`in_progress`/`planned`/`not_applicable` bucket and a pinned Go destination when one exists (e.g. `auth.py`→`cmd/gormes/auth.go`, `cron.py`→`internal/cron/`, `gateway.py`→`cmd/gormes/gateway.go`+`internal/gateway/`, `env_loader.py`→`internal/config/dotenv.go`, `plugins.py`→`internal/plugins/`). `internal/cli/hermes_tree_test.go` guards the manifest invariants (count == 49, no duplicates, valid status enum, real on-disk Go destinations, manifest covers every upstream file, bucket sum matches total). Per-file line-for-line ports (banner, backup, claw, cli_output, clipboard, colors, completion, copilot_auth, curses_ui, dingtalk_auth, dump, logs, nous_subscription, platforms, profiles, setup, skin_engine, status, tips, uninstall, webhook, web_server) and the remaining in-progress surfaces remain explicit follow-on scope under 5.O. |
| 5.P — Docker / Packaging | 🔨 in progress | OCI image landed: `gormes/Dockerfile` now ships a multi-stage `golang:1.26-bookworm` builder + Debian-slim runtime with a CGO-disabled static `./cmd/gormes` binary installed under `/opt/gormes`, exports `GORMES_HOME=/opt/data` + `XDG_DATA_HOME=/opt/data`, declares `VOLUME ["/opt/data"]`, and `docker/entrypoint.sh` bootstraps the canonical `cron/sessions/logs/hooks/memories/skills/plans/workspace/home` layout with `gosu` UID remap and first-run `SOUL.md` bootstrap. Remaining work: `packaging/homebrew` + `scripts/{install.sh,install.cmd,install.ps1,release.py,build_skills_index.py}` ports. |
| 5.Q — TUI Gateway Streaming Surface | 🔨 in progress | `internal/tuigateway/sse.go` now ships the Go-native SSE streaming spine for the future remote TUI gateway: `NewSSEHandler` pumps `kernel.RenderFrame` snapshots over `text/event-stream` (one `event: frame` per frame, JSON-encoded body, terminating `event: end` on input-channel close) with proper `Cache-Control`/`X-Accel-Buffering`/per-flush semantics, and `DialSSE`/`ConsumeSSE` close the loop with a parser that survives keepalive comments, malformed frames, and request-context cancellation. `internal/tuigateway/mux.go` now freezes the path-convention layer: exported `FramesPath`/`EventsPath` constants, `NewRemoteClient(baseURL)` (joins the base URL with the path constants, trims trailing slashes, rejects empty/scheme-less inputs at construction), and `NewGatewayMux(frames, sink)` (mounts the SSE + event handlers at the canonical paths with a 404 for anything else) — both sides share the constants so the wire convention cannot drift. `cmd/gormes/remote.go` now closes the operator-facing seam: `openRemoteTUISurface` composes `NewRemoteClient` with the frames dial into the exact `(<-chan kernel.RenderFrame, tui.Submitter, tui.Canceller)` triple `tui.NewModel` already consumes (Submit/Cancel closures dispatch the POST on a goroutine so the Bubble Tea Update loop is never blocked; errors land on a caller-supplied `*slog.Logger` matching the local seam's fire-and-forget discipline), and `runRemoteTUI` runs Bubble Tea against that surface with the same signal-watched shutdown budget as the local path. `cmd/gormes/main.go` registers a `--remote <url>` string flag on the root command and short-circuits `runTUI` ahead of config/health/session-bolt setup when it's non-empty — the remote gateway owns the LLM health check and session persistence, and invalid or unreachable URLs surface synchronously as a `--remote: ...` error so the startup path bails loudly instead of launching a cosmetic TUI. `cmd/gormes/remote_test.go` guards the end-to-end: frames flow through `NewGatewayMux`→`openRemoteTUISurface` with `Seq`/`DraftText` preserved, Submit/Cancel closures reach the sink, 502-from-sink errors land in the logger instead of panicking, empty/scheme-less inputs and 503 handshakes bail synchronously, the root command exposes a usage-carrying `--remote` flag, and `runTUI --remote not-a-url` fails through the remote surface (not the local LLM health check) so the short-circuit is observable. `internal/tuigateway/toolsummary.go` now ports the pure tool-progress primitives (`tui_gateway/server.py` lines 614–654) as reusable helpers for the forthcoming tool.start/tool.complete JSON-RPC fan-out: `FormatToolDuration(*time.Duration)` renders `""`/`"%.1fs"`/`"Ns"`/`"Xm Ys"`/`"Xm"` ranges matching Python's `_fmt_tool_duration`, `CountList(any, ...string)` walks nested `map[string]any` trees and returns `(len, true)` for lists, `(0, false)` for missing/wrong-type paths, and `(0, true)` for empty lists so `firstCount` can replicate Python's `or` short-circuit, and `ToolSummary(name, result, d)` decodes the JSON result once, renders singular/plural `"Did N search(es)"` from `data.web` (web_search) and `"Extracted N page(s)"` from `results` or `data.results` (web_extract), falls back to `"Completed"` only when a duration is known, appends `" in <FormatToolDuration>"` whenever the duration is set, and returns the empty string when neither applies — matching the Python `None` that suppresses the emission entirely. `internal/tuigateway/toolsummary_test.go` guards the invariants via subtests covering every duration bucket, every CountList shape, and every ToolSummary branch (singular/plural, data.results fallback, decode failures, unknown tools). `internal/tuigateway/image.go` now ports the pure image-attachment primitives (`tui_gateway/server.py` lines 155–178) so the forthcoming image-upload JSON-RPC method has a typed, tested surface: `EstimateImageTokens(width, height int) int` mirrors `_estimate_image_tokens` (non-positive dims short-circuit to 0; positive dims tile into 512×512 blocks via `(n+511)/512` with a `max(1, …)` floor and multiply by 85 tokens/tile), `ImageMeta{Name, Width, Height, TokenEstimate}` mirrors the upstream dict shape with `,omitempty` JSON tags so a decode failure emits `{"name":"…"}` byte-identical to the Python fallback, and `ReadImageMeta(path string) ImageMeta` decodes via stdlib `image.DecodeConfig` (PNG/JPEG/GIF registrations) so open-errors and non-image payloads both fall through to the Name-only struct. `internal/tuigateway/image_test.go` guards the invariants: EstimateImageTokens covers zero/negative dims, the 1×1/512×512/513×513/1024×1024 tile boundaries, a 1025×2049 (3×5 tiles) case, and a 2048×256 wide-short case; ReadImageMeta covers PNG/JPEG round-trips, a missing-file path that still surfaces the basename, and a text file that decode-errors into Name-only fallback; a JSON marshal test pins both the Name-only and fully-populated omitempty shapes. Broader `tui_gateway/` server.py port (slash worker, skin engine, individual JSON-RPC methods, image-upload wiring, approvals) and the `hermes_cli/skin_engine.py` skin engine remain explicit follow-on scope under 5.Q. |

## Relationship to Phase 6

Phase 5.F (Skills System port) is the mechanical work: porting the upstream Python skills plumbing. Phase 6 (The Learning Loop) is the algorithm on top — detecting complexity, distilling patterns, scoring feedback. Phase 5.F is a dependency of Phase 6, but they are not the same work.
