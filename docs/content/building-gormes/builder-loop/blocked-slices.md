---
title: "Blocked Slices"
weight: 40
aliases:
  - /building-gormes/blocked-slices/
---

# Blocked Slices

This page is generated from canonical `progress.json` rows that declare
`blocked_by`.

Use it to avoid assigning work before the dependency chain is ready.

<!-- PROGRESS:START kind=blocked-slices -->
| Phase | Slice | Blocked by | Ready when | Unblocks |
|---|---|---|---|---|
| 2 / 2.B.5 | BlueBubbles iMessage session-context prompt guidance | BlueBubbles iMessage bubble formatting parity | BlueBubbles outbound formatting splits blank-line paragraphs into separate iMessage sends, so prompt guidance has a matching delivery contract. | - |
| 2 / 2.B.5 | WhatsApp unsafe alias endpoint inbound evidence | WhatsApp unsafe sender/chat inbound evidence | WhatsApp unsafe sender/chat inbound evidence is complete on main., The worker extends only the inbound safety fixture around alias endpoints; identity.go and existing identity_contract.json safe cases are read-only., No bridge/native runtime, send/reconnect path, gateway manager dispatch, or Hermes lid-mapping file is needed. | - |
| 2 / 2.B.5 | Telegram group require-mention bot binding | Telegram group bot-command mention gate helper, Telegram require-mention config fields | Telegram require-mention config fields and Telegram group bot-command mention gate helper are validated on main., The worker can build synthetic tgbotapi.Update values and a Bot/Config fixture; no live getMe call, token, gateway.Manager, pairing store, or provider runtime is required., The binding remains opt-in and must leave existing allowed_chat_id, first_run_discovery, fresh-final delete, and command parsing tests unchanged. | - |
| 2 / 2.B.5 | Gateway inbound dedup manager binding | Gateway inbound dedup key helper, Gateway message deduplicator bounded helper | Gateway inbound dedup key helper and Gateway message deduplicator bounded helper are validated on main; treat inbound_dedup_key.go and message_deduplicator.go as read-only in this row., The worker can use fake gateway.Channel and fake kernel fixtures only; no Telegram, Slack, Discord, provider SDK, or platform adapter edits are needed., ManagerConfig can add only the minimum in-memory dedup field or dependency injection needed for tests; no disk store, package-global cache, event schema change, or platform-specific state is required. | - |
| 2 / 2.F.5 | Steer slash command registry + queue fallback | Human review: steer queue manager hook | Steer slash command parser + preview helper is validated on main., Human review identifies whether /steer should reuse queueFollowUpIfActive, a separate steer guidance queue, or the existing CommandActiveTurnPolicyDrain path., After review, the worker must keep the parser helper read-only and add only registry/queue fallback behavior; live between-tool-call injection remains a dependent row., Tests must use fake running-agent state and fake command dispatch; no provider, active tool loop, TUI, Slack, or Telegram transport is required. | Mid-run steer injection between tool calls, Gateway-handled slash commands bypass active-session guard |
| 4 / 4.A | Codex OAuth state + stale-token relogin | Token vault, Multi-account auth, Codex Responses pure conversion harness, Codex Responses assistant content role types | Gormes has an XDG-scoped token vault and account-selection seam for provider credentials. | - |
| 4 / 4.B | Kernel compression-boundary callback binding | ContextEngine compression-boundary callback vocabulary, Context compression | ContextEngine compression-boundary callback vocabulary is complete on main., The worker identifies the existing kernel compression execution point; if no explicit compression execution path exists in the checkout, keep this row blocked and do not create fake wiring., A fake ContextEngine can record boundary notifications without calling a live provider or mutating real transcript storage. | - |
| 4 / 4.F | Auto-naming sessions | Title prompt and truncation contract, Title auxiliary failure visibility | Title prompt and truncation contract and Title auxiliary failure visibility are validated on main., Session metadata write helpers can be exercised with temp stores; no live provider or TUI program is required. | - |
| 4 / 4.G | Anthropic OAuth/keychain credential discovery | Token vault | Token vault owns XDG-scoped credential files and can expose provider auth status without live credentials. | - |
| 4 / 4.G | Google OAuth flow + refresh seam | Token vault | Token vault is planned but not required for this pure state-machine slice; tests inject token/client-secret paths under t.TempDir., The worker can model OAuth state and token payloads without importing Google SDKs, opening a browser, making HTTP requests, or touching provider adapters., The row is about Google Workspace skill auth and future Google provider setup, not Google Code Assist model routing. | - |
| 5 / 5.B | Terminal snapshot source stdout suppression guard | Environment interface + file sync contract | Environment interface + file sync contract has landed and exposes a shell-wrapper construction seam before backend-specific execution is enabled., Tests can use string-built shell wrapper fixtures and fake snapshots; no real shell, macOS bash, Docker, SSH, or sandbox backend is required., The row is about terminal environment wrapper construction only, not execute_code or durable worker shell jobs. | - |
| 5 / 5.F | Skills hub direct URL install name/category guard | Skills hub direct URL candidate parser, SKILL.md frontmatter validation guard | Skills hub direct URL candidate parser is validated and returns stable missing-name/invalid-name evidence., SKILL.md frontmatter validation guard is validated so malformed URL skills are excluded before prompt injection., The worker can use an injected fetcher and fake quarantine/scan result; no live HTTP, GitHub registry, or prompt-selection runtime is required. | - |
| 5 / 5.I | Hindsight memory setup blank-input preservation | Third-party extensions | Third-party extensions have a native plugin config-patch seam; do not implement this before the generic plugin setup surface exists., Tests can exercise a pure config patch helper with in-memory maps and temp files; no Hindsight cloud/local daemon, uv install, Python plugin import, or Goncho memory change is required., This row is external memory-plugin compatibility only; internal Goncho remains the Go-native Honcho-compatible memory implementation. | - |
| 5 / 5.J | Subagent dangerous-command non-interactive approval policy | Recoverable dangerous patterns + blocked-result schema, Approval mode config normalization | Dangerous-command detection and approval-mode config normalization are fixture-locked for local tools. | - |
| 5 / 5.N | Debug share paste sweep scheduler contract | Debug helpers | Debug helpers are split into a Go-native diagnostic surface with redaction and no hidden writes., A native debug-share pending-paste queue exists or is specified as a pure helper before scheduler wiring begins., Tests can use fake clock and fake deleter; no network DELETE, paste.rs upload, gateway process, or live cron daemon is required. | - |
| 5 / 5.P | Unix installer root/FHS layout policy | Unix installer (install.sh) source-backed update flow | Unix installer (install.sh) source-backed update flow has canonical scripts under scripts/ and a byte-equal served site copy. | Installer site asset/route coverage |
| 5 / 5.Q | Native TUI renderConv viewport budget binding | Native TUI conversation viewport tail helper | Native TUI conversation viewport tail helper is complete and tested., The worker only binds renderConv/view rendering to the helper; kernel.RenderFrame, session persistence, transcript storage, remote TUI SSE, and API server behavior are read-only., Tests can call renderConv or a package-private wrapper directly without starting a Bubble Tea program. | - |
| 5 / 5.Q | API server cron admin read-only endpoints | API server detailed health endpoint | API server detailed health endpoint is complete., Cronjob tool action envelope over native store, Cron context_from output chaining, and Cron multi-target delivery + media/live-adapter fallback are validated on main., Tests can seed the native cron store/run audit directly; no scheduler goroutine, live delivery adapter, provider, or cron mutation endpoint is required. | API server cron admin mutating endpoints |
| 5 / 5.Q | API server cron admin mutating endpoints | API server cron admin read-only endpoints | API server cron admin read-only endpoints is complete and exposes the same cron store/read model., Cron prompt/script safety + pre-run script contract, Cronjob tool action envelope over native store, and Cron multi-target delivery + media/live-adapter fallback are validated on main., Tests can inject fake delivery/action handlers; no live gateway adapter, provider, or wall-clock scheduler tick is required. | - |
| 6 / 6.B | LLM-assisted pattern distillation | Portable SKILL.md format | Portable SKILL.md format is validated, so generated drafts can carry review state and remain out of prompt injection until approved., The row can use fake model/review prompt fixtures; no live provider, skill install, or prompt injection is required. | - |
| 6 / 6.C | SKILL.md frontmatter validation guard | Phase 2.G skills runtime | Phase 2.G skills runtime is complete and internal/skills/parser.go already requires SKILL.md frontmatter delimiters., This row is validation-only: workers should add structured error codes and parser/status evidence before versioned metadata, atomic writes, auto-fix, or hook installation., The worker can use temp SKILL.md files and path-derived slug fixtures; no GBrain CLI, pre-commit hook, live embedding, or learning-loop extractor is required. | Portable SKILL.md format |
| 6 / 6.C | Hermes creative skill metadata compatibility | SKILL.md frontmatter validation guard | SKILL.md frontmatter validation guard is validated so malformed creative skill files are excluded before compatibility metadata is trusted., The row uses embedded string fixtures copied from the upstream frontmatter/body decision table only; no live skill install, image generation, browser artifact build, semantic embedding, or network fetch is required., The existing Phase 2.G parser/store and selector tests remain the baseline; this row only expands metadata shape and route evidence. | Portable SKILL.md format |
| 6 / 6.C | Portable SKILL.md format | SKILL.md frontmatter validation guard, Hermes creative skill metadata compatibility, Phase 2.G skills runtime | SKILL.md frontmatter validation guard is validated so malformed skill files are excluded before review metadata is interpreted., Hermes creative skill metadata compatibility is validated so versioned metadata preserves real upstream skill routing rather than an invented generic design category., Phase 2.G skills runtime is complete and the parser/store seam is stable enough for versioned metadata. | LLM-assisted pattern distillation, Hybrid lexical + semantic lookup, Skill effectiveness scoring |
| 6 / 6.D | Hybrid lexical + semantic lookup | Portable SKILL.md format | Portable SKILL.md format is validated so skills carry review state, triggers, exclusions, and provenance metadata., Phase 3 semantic embedding lookup remains optional and can be stubbed in unit fixtures without Ollama. | Source-aware retrieval damping fixtures, Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Source-aware retrieval damping fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and returns score explanations that can carry source-tier evidence., The worker can use synthetic skill/memory sources and fake scores; no GBrain SQL engine, PGLite, Postgres, or tree-sitter runtime is required. | Delta-bounded skill and memory maintenance passes, Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Delta-bounded skill and memory maintenance passes | Source-aware retrieval damping fixtures | Hybrid lexical + semantic lookup and Source-aware retrieval damping fixtures are validated so maintenance evidence can reuse score explanations and source tiers., The worker can use fake skill/memory rows and injected stale-count callbacks; no live embeddings, Ollama, Postgres/PGLite, or tree-sitter runtime is required. | Skill effectiveness scoring |
| 6 / 6.D | Code Cathedral II code-context retrieval fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and can accept optional external evidence in fixtures. | - |
| 7 / 7.C | Matrix self/bridge sender drop helper | Matrix shared-chassis bot seam | Matrix shared-chassis bot seam exists so the helper can live under internal/channels/matrix without inventing a parallel gateway adapter., The slice is pure string classification plus table tests; no live Matrix sync, login, pairing store, E2EE store, room join, or gateway manager behavior is required., Tests should cover sender localpart parsing with Matrix IDs only; no mautrix/nio SDK dependency is needed. | Matrix real client/bootstrap layer |
| 7 / 7.C | Matrix E2EE device-id crypto-store binding | Matrix real client/bootstrap layer | Matrix real client/bootstrap layer has auth, sync/invite handling, room-kind policy, and a fakeable E2EE bootstrap seam. | - |
| 7 / 7.E | Yuanbao media/sticker attachment normalization | Yuanbao protocol envelope + markdown fixtures | Yuanbao protocol envelope + markdown fixtures is fixture-ready or validated., The row can use static media/sticker metadata fixtures and t.TempDir files; no live download, CDN auth, or sticker decoding service is required., Gateway attachments already have a degraded evidence path for unavailable media. | Yuanbao gateway runtime + toolset registration |
| 7 / 7.E | Yuanbao gateway runtime + toolset registration | Yuanbao protocol envelope + markdown fixtures, Yuanbao media/sticker attachment normalization, Platform toolset config persistence + MCP sentinel | Yuanbao protocol envelope + markdown fixtures and Yuanbao media/sticker attachment normalization are fixture-ready or validated., Shared gateway manager registration and platform toolset config persistence are validated on main., The first runtime slice may use fake login/send clients only; live QR/session handling remains disabled until operator config and docs are present. | - |
<!-- PROGRESS:END -->
