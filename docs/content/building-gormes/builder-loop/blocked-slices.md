---
title: "Blocked Slices"
weight: 40
aliases:
  - /building-gormes/blocked-slices/
---

# Blocked Slices

This page is generated from canonical `progress.json` rows that declare
`blocked_by`.

Use it to avoid assigning work before the dependency chain is ready.

<!-- PROGRESS:START kind=blocked-slices -->
| Phase | Slice | Blocked by | Ready when | Unblocks |
|---|---|---|---|---|
| 2 / 2.B.5 | BlueBubbles iMessage session-context prompt guidance | BlueBubbles iMessage bubble formatting parity | BlueBubbles outbound formatting splits blank-line paragraphs into separate iMessage sends, so prompt guidance has a matching delivery contract. | - |
| 2 / 2.F.5 | Steer slash command registry + queue fallback | 2.E.2 | 2.E.2 is complete and the shared CommandDef registry is stable for gateway commands. | Mid-run steer injection between tool calls, Gateway-handled slash commands bypass active-session guard |
| 4 / 4.A | Azure Foundry transport probe read model | Azure Foundry probe — path sniffing, Azure Foundry probe — /models classification + Anthropic fallback | Azure OpenAI query/default_query transport contract and Azure Anthropic Messages endpoint contract are validated, so each detected transport has a request-builder contract., The worker can implement pure probe functions with injected fake HTTP responses; no config mutation or provider runtime resolver is needed. | Azure Foundry runtime env/config read model |
| 4 / 4.A | Azure Foundry runtime env/config read model | Azure Foundry transport probe read model, Provider-enforced context-length resolver | Azure Foundry transport probe read model is fixture-ready or validated, so this row can consume a typed probe result instead of reimplementing transport detection., Provider-enforced context-length resolver is validated and can resolve model context from pure registry fixtures. | Azure Foundry CLI setup/status manual fallback |
| 4 / 4.A | Azure Foundry CLI setup/status manual fallback | Azure Foundry runtime env/config read model | Azure Foundry runtime env/config read model is validated and can emit redacted operator evidence., CLI command registry parity or a focused internal/cli helper seam can render provider setup/status without broad command wiring. | - |
| 4 / 4.A | Bedrock stale-client eviction + retry classification | Bedrock SigV4 + credential seam | A Bedrock client/cache seam exists behind the provider adapter and can be exercised without live AWS credentials. | - |
| 4 / 4.A | Codex OAuth state + stale-token relogin | Token vault, Multi-account auth, Codex Responses pure conversion harness, Codex Responses assistant content role types | Gormes has an XDG-scoped token vault and account-selection seam for provider credentials. | - |
| 4 / 4.G | Anthropic OAuth/keychain credential discovery | Token vault | Token vault owns XDG-scoped credential files and can expose provider auth status without live credentials. | - |
| 4 / 4.H | Provider rate guard + budget telemetry | Provider-side resilience, Classified provider-error taxonomy, Retry-After header parsing + HTTPError hint, Provider rate guard — x-ratelimit header classification, Provider rate guard — degraded-state + last-known-good evidence | Provider-side resilience, classified provider-error taxonomy, and Retry-After hint handling are validated, so this row only adds rate-guard/budget decisions and status evidence., The worker can use fake headers and fake clock fixtures; no live Nous Portal, provider key, or wall-clock sleep is required. | - |
| 4 / 4.H | Provider rate guard — degraded-state + last-known-good evidence | Provider rate guard — x-ratelimit header classification | Provider rate guard — x-ratelimit header classification is fixture_ready or validated, so this slice can import RateLimitClass. | Provider rate guard + budget telemetry |
| 5 / 5.F | Skills hub search read-model function over registry providers | Skills hub search result types + in-memory registry provider | Skills hub search result types + in-memory registry provider is validated so the type seam is stable., The slice can use only the in-memory fake provider; no gateway command wiring, no install flow, and no prompt-injection are part of this row. | Skill registries, TUI + Telegram browsing |
| 5 / 5.G | MCP stdio transport + tool/list discovery | MCP server config/env resolver | internal/tools/mcp_config.go already provides MCPServerDefinition with Command/Args/Env and the stdio transport constant., Tests inject a `Conn io.ReadWriteCloser` instead of spawning a real subprocess; an in-process bytes.Buffer-based fake server can echo JSON-RPC frames. | MCP HTTP transport + tool/list discovery, MCP schema normalization + structured-content adapter |
| 5 / 5.G | MCP HTTP transport + tool/list discovery | MCP stdio transport + tool/list discovery | `MCP stdio transport + tool/list discovery` is complete (MCPRawTool and the typed error set are defined)., MCPServerDefinition.URL + Headers are present in mcp_config.go for HTTP servers., Tests construct an httptest.Server returning canned JSON-RPC responses; no real network destination is used. | MCP schema normalization + structured-content adapter, MCP OAuth state store + noninteractive auth errors |
| 5 / 5.G | MCP schema normalization + structured-content adapter | MCP stdio transport + tool/list discovery, MCP HTTP transport + tool/list discovery | `MCP stdio transport + tool/list discovery` and `MCP HTTP transport + tool/list discovery` are both complete (MCPRawTool exists and both clients return a stable shape)., internal/tools/tool.go provides the native Tool descriptor that NormalizeTools targets. | Managed tool gateway bridge |
| 5 / 5.G | MCP OAuth refresh + 401 session-expired recovery | MCP OAuth state store + noninteractive auth errors | MCP OAuth state store + noninteractive auth errors is validated, exporting MCPOAuthStore/MCPOAuthToken/ErrMCPOAuthNoninteractiveRequired. | Managed tool gateway bridge |
| 5 / 5.G | Managed tool gateway bridge | MCP schema normalization + structured-content adapter, MCP OAuth state store + noninteractive auth errors | MCP stdio + HTTP transports, MCP schema normalization + structured-content adapter, and MCP OAuth state store + noninteractive auth errors are all validated on main. | - |
| 5 / 5.J | Recoverable dangerous patterns + blocked-result schema | Hardline command pattern table + DetectHardline function | Hardline command pattern table + DetectHardline function is validated so DetectHardline can be called from GuardCommand., The slice owns one new file plus its test; execute_code.go and the registry are not touched in this row. | Approval mode config normalization, Subagent dangerous-command non-interactive approval policy, Cron dangerous-command approval mode, Tirith, path, URL, and website policy integration, Wire GuardCommand into execute_code boundary |
| 5 / 5.J | Subagent dangerous-command non-interactive approval policy | Recoverable dangerous patterns + blocked-result schema, Approval mode config normalization | Dangerous-command detection and approval-mode config normalization are fixture-locked for local tools. | - |
| 5 / 5.N | Cron context_from output chaining | Cronjob tool API + schedule parser parity | Cronjob tool API + schedule parser parity has a create/update/list surface over the Go cron store, or this slice owns the minimal ContextFrom field and prompt-builder fixture without exposing a public tool yet. | - |
| 5 / 5.O | CLI webhook URL normalizer | CLI deterministic tip selector | CLI deterministic tip selector lands so internal/cli has a stable surface; this row only adds one pure function with sentinel errors. | CLI command registry parity + active-turn busy policy |
| 5 / 5.O | CLI command registry parity + active-turn busy policy | CLI deterministic tip selector, CLI webhook URL normalizer, CLI dump support-summary helper | CLI deterministic tip selector, CLI webhook URL normalizer, and CLI dump support-summary helper are validated, so command registry output can reuse the three pure helper layers. | Busy command guard for compression and long CLI actions, Gateway, platform, webhook, and cron management CLI, Diagnostics, backup, logs, and status CLI |
| 5 / 5.O | Gateway /reasoning apply + dispatch | Gateway /reasoning command parser | Gateway /reasoning command parser is fixture_ready or validated., internal/gateway/manager.go already has a command-dispatch seam this slice can extend without a registry refactor. | CLI command registry parity + active-turn busy policy |
| 5 / 5.O | Busy command guard for compression and long CLI actions | CLI command registry parity + active-turn busy policy | The CLI command registry has a shared active-turn/busy policy surface. | - |
| 5 / 5.O | CLI profile root resolver | CLI profile name validator | CLI profile name validator is validated, exporting ValidateProfileName. | CLI active-profile store |
| 5 / 5.O | CLI active-profile store | CLI profile name validator, CLI profile root resolver | CLI profile name validator and CLI profile root resolver are validated, exporting ValidateProfileName/ResolveProfileRoot. | CLI auth status read model before provider setup, Setup/uninstall dry-run command contracts |
| 5 / 5.O | Gateway mutating-subcommand unavailability stub | Gateway management CLI read-model closeout | Gateway management CLI read-model closeout is complete (read-only baseline already landed)., internal/cli/service_restart.go provides the systemd/launchd helper that the unavailability message points to. | Service restart active-status poller, Webhook/platform management CLI helpers |
| 5 / 5.O | CLI log snapshot reader using shared redactor | CLI log redactor for known secret shapes | CLI log redactor for known secret shapes is fixture_ready or validated so this slice can import RedactLine., Tests use t.TempDir() and inject LogSnapshotRoots — no XDG resolution inside this helper. | CLI status summary over native stores, Backup manifest dry-run contract |
| 5 / 5.P | Unix installer root/FHS layout policy | Unix installer (install.sh) source-backed update flow | Unix installer (install.sh) source-backed update flow has canonical scripts under scripts/ and a byte-equal served site copy. | Installer site asset/route coverage |
| 5 / 5.Q | TUI gateway completion path normalizer | TUI gateway tool-progress mode normalizer | TUI gateway tool-progress mode normalizer is fixture-ready or validated, so the internal/tuigateway package directory already exists., Pure-string transformation only — table tests with synthetic inputs. | TUI gateway image/personality/platform-event helpers |
| 5 / 5.Q | TUI gateway tool summary formatter | TUI gateway tool-progress mode normalizer | TUI gateway tool-progress mode normalizer is fixture-ready or validated, so the internal/tuigateway package directory already exists., Pure JSON-shape formatting only — table tests with synthetic resultJSON values. | SSE streaming to Bubble Tea TUI |
| 5 / 5.Q | TUI gateway image/personality/platform-event helpers | TUI gateway completion path normalizer | TUI gateway progress/completion helpers are validated, so internal/tuigateway package scaffolding exists. | SSE streaming to Bubble Tea TUI |
| 5 / 5.Q | Native TUI /save canonical session export | Native TUI slash-command dispatch table | `Native TUI slash-command dispatch table` is complete (the registry and /save stub already exist)., internal/transcript.ExportMarkdown is the canonical persisted-transcript reader — `cmd/gormes/session.go` already proves it works over a sql.DB pointing at config.MemoryDBPath(). | - |
| 5 / 5.Q | TUI running-agent placeholder surfaces interrupt + queued slash actions | Native TUI slash-command dispatch table | `Native TUI slash-command dispatch table` is complete (the registry is the source of truth for which slashes show in the busy placeholder)., internal/tui/update.go already toggles `m.inFlight` between user submit and PhaseIdle frame; this slice piggybacks on that boolean. | - |
| 5 / 5.Q | SSE streaming to Bubble Tea TUI | TUI gateway image/personality/platform-event helpers, TUI launch model override + static alias resolver | TUI gateway image/personality/platform-event helpers are validated., TUI launch model override + static alias resolver is either validated or deliberately excluded from remote startup in this slice. | Dashboard PTY chat sidecar contract |
| 5 / 5.Q | Dashboard PTY chat sidecar contract | PTY bridge protocol adapter, SSE streaming to Bubble Tea TUI | PTY bridge behavior and TUI gateway event streaming are each fixture-locked. | - |
| 5 / 5.Q | API server health + cron admin endpoints | Cronjob tool API + schedule parser parity, Cron prompt/script safety + pre-run script contract, Cron multi-target delivery + media/live-adapter fallback | Cronjob tool API + schedule parser parity, Cron prompt/script safety + pre-run script contract, and Cron multi-target delivery + media/live-adapter fallback have stable native store contracts. | - |
| 6 / 6.B | LLM-assisted pattern distillation | Portable SKILL.md format | Portable SKILL.md format is validated, so generated drafts can carry review state and remain out of prompt injection until approved., The row can use fake model/review prompt fixtures; no live provider, skill install, or prompt injection is required. | - |
| 6 / 6.C | Portable SKILL.md format | Phase 2.G skills runtime | Phase 2.G skills runtime is complete and the parser/store seam is stable enough for versioned metadata. | LLM-assisted pattern distillation, Hybrid lexical + semantic lookup, Skill effectiveness scoring |
| 6 / 6.D | Hybrid lexical + semantic lookup | Portable SKILL.md format | Portable SKILL.md format is validated so skills carry review state, triggers, exclusions, and provenance metadata., Phase 3 semantic embedding lookup remains optional and can be stubbed in unit fixtures without Ollama. | Source-aware retrieval damping fixtures, Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Source-aware retrieval damping fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and returns score explanations that can carry source-tier evidence., The worker can use synthetic skill/memory sources and fake scores; no GBrain SQL engine, PGLite, Postgres, or tree-sitter runtime is required. | Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Code Cathedral II code-context retrieval fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and can accept optional external evidence in fixtures. | - |
| 7 / 7.C | Matrix E2EE device-id crypto-store binding | Matrix real client/bootstrap layer | Matrix real client/bootstrap layer has auth, sync/invite handling, room-kind policy, and a fakeable E2EE bootstrap seam. | - |
<!-- PROGRESS:END -->
